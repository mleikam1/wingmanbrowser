/// Release metadata shared by UI and signed policy compatibility checks.
/// A test verifies this matches pubspec.yaml, which supplies native build metadata.
abstract final class AppVersion {
  static const name = '0.13.0';
  static const build = '14';
}
